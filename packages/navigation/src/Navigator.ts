/**
 * Navigator.ts — Re-exports for backward compatibility.
 * The Navigator is now built directly inside Router.createRouter().
 */

// Navigator is now inline in Router.ts — nothing to export here.
