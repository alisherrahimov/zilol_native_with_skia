platform :ios, '15.0'

target 'ZilolNative' do
  # ── All dependencies built from source ─────────────────────────────
  # Hermes  — github.com/facebook/hermes  → vendor/hermes/
  # Yoga    — github.com/facebook/yoga    → vendor/yoga/
  # Skia    — github.com/google/skia      → vendor/skia/
  # All linked directly via project.yml, zero pods needed.
end
